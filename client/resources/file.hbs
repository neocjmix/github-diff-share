{{#each this}}
    <article>
        <header>
            <h1>{{from}} -&gt; {{to}}</h1>
        </header>
        <main class="content">
            <ul class="chunks">
                {{#each this.chunks}}  
                    <li>
                        <code>
                            {{#each this.changes}}
                                <span class="row {{type}}">
                                    <span class="cell" data-line-number="{{lineNumber}}">{{content}}</span>
                                </span>
                            {{/each}}
                        </code>
                    </li>
                {{/each}}
            </ul>
        </main>
    </article>
{{/each}}